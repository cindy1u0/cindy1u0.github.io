import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.section`
  height: 100vh;
`;

const Photography = (): JSX.Element => {
  return <Wrapper>Photography</Wrapper>;
};

export default Photography;
